import { WhitespaceValidators } from './whitespacevalidators';

describe('Validators', () => {
  it('should create an instance', () => {
    expect(new WhitespaceValidators()).toBeTruthy();
  });
});
